[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fluorescence-model"
version = "0.1.0"
description = "Fluorescence excitation/emission spectral modeling for filter and LED/laser source selection"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
